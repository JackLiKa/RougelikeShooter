using UnityEngine;

/*
 * [AI_COMMENTED_OUT]
 * 下面这份 MainMenuButtons 原始实现保留为注释，方便以后对照旧逻辑。
 * 现在主菜单逻辑已经迁移到 MainMenuGameLoop -> Facade -> UIController -> PanelRoot，
 * 不再依赖挂在 MainMenu 根节点上的旧脚本。
 *
 * public class MainMenuButtons : MonoBehaviour
 * {
 *     ...
 * }
 */

public class MainMenuButtons : MonoBehaviour
{
    // [AI_EXTENSION] Legacy component kept as a harmless stub so existing scene references do not break.
}
